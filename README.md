# Hoox Email Worker

This worker is responsible for scanning emails (via IMAP or Webhooks) and triggering trading signals.

## Configuration

Update `wrangler.jsonc` or `.dev.vars` with necessary credentials.
